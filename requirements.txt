fastapi==0.110.0          # Web framework (includes Starlette & CORS middleware you use)
uvicorn[standard]==0.29.0 # ASGI server (the [standard] extra pulls in uvloop & httptools for prod)
pydantic==2.11.5          # Data-validation & models
pydantic-settings==2.9.1  # Settings management (`from pydantic_settings import BaseSettings`)
duckdb==1.2.2             # MotherDuck-compatible database driver
python-dotenv==1.0.0      # Handy for loading .env files locally (optional but common)
